const path = require("path");

module.exports = {
  config: path.resolve("src/config/config.js"), // ✅ Fix config path
  "models-path": path.resolve("src/models"),
  "migrations-path": path.resolve("src/migrations"),
};
